import { ForbiddenTitleDirective } from './forbidden-title.directive';

describe('ForbiddenTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenTitleDirective();
    expect(directive).toBeTruthy();
  });
});
