import { ForbiddenLastNameDirective } from './forbidden-last-name.directive';

describe('ForbiddenLastNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenLastNameDirective();
    expect(directive).toBeTruthy();
  });
});
